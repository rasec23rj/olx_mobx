import 'package:flutter/material.dart';

class AninciosPage extends StatelessWidget {
  const AninciosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(
      builder: (_, constrains) {
        return Container();
      },
    );
  }
}
